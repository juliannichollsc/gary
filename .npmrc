# GARY uses pnpm (migrated from npm). Content-addressable store + strict, isolated node_modules
# (mitigates supply-chain / phantom-dependency issues).
auto-install-peers=true
strict-peer-dependencies=false
# keep the lockfile authoritative in CI
# frozen-lockfile is passed explicitly in CI, not globally
